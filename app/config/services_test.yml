parameters:
    enable_labels: true
    # token generated from a fake account
    github_token: dc3541f1283279b880f9a733fbb3dd90e48b2fcf
    repository_username: mickaelandrieu
    repository_name: test-modobot
    
imports:
    - { resource: services.yml }

services:
    app.status_api:
        class: AppBundle\Tests\Issues\NullStatusApi
    # Your integrations tests shouldn't post comments to github
    app.pullrequest_listener:
        class: AppBundle\Tests\PullRequests\FakeListener
        arguments: ['@app.comment_api', '@validator', '@twig']